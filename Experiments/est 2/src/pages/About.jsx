function About() {
  return (
    <section>
      <h2>About</h2>
      <p>This application uses React Router for navigation and React.lazy for route-level code splitting.</p>
      <p>Suspense handles loading states while each page bundle is fetched.</p>
    </section>
  );
}

export default About;
